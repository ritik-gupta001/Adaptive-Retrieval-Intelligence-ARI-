# ARI Platform — direct runtime dependencies (unpinned)
# Generated from pyproject.toml [project.dependencies]
#
# For reproducible installs with pinned transitive deps, use:
#   pip install -r requirements/base.txt    # core runtime
#   pip install -r requirements/prod.txt    # + BGE reranker + SQLite checkpointer
#   pip install -r requirements/dev.txt     # + test tools
#   pip install -r requirements/eval.txt    # + RAGAS + DeepEval
#
# Or install the package directly:
#   pip install -e .          # core
#   pip install -e ".[prod]"  # production extras
#   pip install -e ".[all]"   # everything

langgraph>=0.2.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-anthropic>=0.2.0
langchain-openai>=0.2.0
langchain-chroma>=0.1.0
anthropic>=0.34.0
openai>=1.40.0
chromadb>=0.5.0
rank-bm25>=0.2.2
tavily-python>=0.5.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
pyyaml>=6.0.0
python-dotenv>=1.0.0
python-json-logger>=2.0.0
langsmith>=0.1.0
python-multipart>=0.0.18
pypdf>=5.0.0
